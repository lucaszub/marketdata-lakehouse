name: 'marketdata'
version: '1.0.0'

profile: 'marketdata'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  marketdata:
    staging:
      +materialized: view
    marts:
      +materialized: table
